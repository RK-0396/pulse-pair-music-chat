# PulsePair environment variables
# Copy this to .env.local for local development

# For local dev the socket connects to the same Next.js server via /api/socket
# No NEXT_PUBLIC_WS_URL needed — the socket path is always /api/socket
